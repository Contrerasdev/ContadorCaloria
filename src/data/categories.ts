//Importando los types para vincular con los datos
import type { Category } from "../Types"

const categories : Category[] = [
    { id: 1, name: 'Comida'},
    { id: 2, name: 'Ejercicio'}
  ]
  export default categories;